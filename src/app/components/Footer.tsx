import {
    useState,
    Fragment,
    ReactNode
} from "react";

import Wrapper from "./Wrapper";

const Footer = () => {
    return (
        <Fragment>
            <Wrapper></Wrapper>
        </Fragment>
    );
};

export default Footer;